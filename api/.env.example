# API environment variables
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/learn_to_cloud
CLERK_SECRET_KEY=sk_test_xxx
CLERK_WEBHOOK_SIGNING_SECRET=whsec_xxx

# Environment mode: "development" or "production"
# - development: Allows localhost CORS, relaxes LABS_VERIFICATION_SECRET requirement
# - production: Requires LABS_VERIFICATION_SECRET, stricter CORS, production caching
ENVIRONMENT=development

# Database debug logging (very verbose - logs every SQL query)
# DB_ECHO=false

# Azure Monitor (optional - only needed for Azure deployment)
# APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=xxx;IngestionEndpoint=xxx

# GitHub API (optional - for higher rate limits)
# GITHUB_TOKEN=ghp_xxx

# Google Gemini API (required for LLM features)
GOOGLE_API_KEY=your_google_api_key_here
# GEMINI_MODEL=gemini-2.0-flash  # Optional, defaults to gemini-2.0-flash

# LLM CLI for AI-powered code analysis (Phase 3/5 verification)
# Two modes:
#   - Stdio mode (local dev): Set LLM_CLI_PATH to CLI binary path
#   - HTTP mode (production): Set LLM_CLI_URL to sidecar URL
# LLM_CLI_PATH=/path/to/copilot-cli
# LLM_CLI_URL=http://localhost:4321
#
# BYOK provider (required for code analysis):
# LLM_BASE_URL=https://<resource>.openai.azure.com
# LLM_API_KEY=your-api-key
# LLM_MODEL=gpt-5-mini

# Frontend URL for CORS (default: http://localhost:4280)
# FRONTEND_URL=http://localhost:3000
