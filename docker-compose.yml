services:
  # Just PostgreSQL for local development
  # Frontend: npm run dev (in /frontend)
  # API: swa start (uses Azure Functions CLI)
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=learn_to_cloud
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Test service to reproduce multi-worker migration race condition
  # Usage: docker compose up db api-multiworker --build
  api-multiworker:
    build:
      context: .
      dockerfile: api/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/learn_to_cloud
      - GITHUB_TOKEN=test_token
      - LLM_API_KEY=test_key
      - AZURE_APP_INSIGHTS_CONNECTION_STRING=
    depends_on:
      db:
        condition: service_healthy
    # Uses default CMD from Dockerfile which runs with 2 workers

volumes:
  postgres_data:
