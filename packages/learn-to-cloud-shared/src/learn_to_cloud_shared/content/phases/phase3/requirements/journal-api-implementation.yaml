# yaml-language-server: $schema=../../../schemas/requirement.schema.json
uuid: d6201101-8873-447a-957a-0e5773627618
slug: journal-api-implementation
submission_type: journal_api_verifier
name: Verify Journal API Implementation
description: >-
  In your public fork, open Actions > Verify capstone > Run workflow and select
  `main`. The latest `.github/workflows/verify-capstone.yml` run must succeed
  for your current `main` commit before you submit. Rerun it after every new
  commit. Ordinary CI does not run all exercises and is not enough to pass.
  The app checks this workflow result, not your source code with an AI reviewer.
  Live AI verification and running your cloud CLI remain local responsibilities;
  this offline workflow does not verify them.
type_config:
  required_repo: learntocloud/journal-starter
