{
  "servers": {
    "playwright": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--init",
        "--pull=always",
        "--network=host",
        "mcr.microsoft.com/playwright/mcp"
      ]
    }
  }
}
